## jest issue with default imports

This replit shows how the issue disappers if a named export is imported, instead of a default one.

### Instructions

```shell
npx lerna run build
npx lerna run test
```
